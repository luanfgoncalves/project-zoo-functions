const data = require('../data/zoo_data');

// function getSpecies(...ids) {
//   const expected = [];
//   if (!ids) {
//     return expected;
//   }
//   const filterCondition = (element) => ids.includes(element.id);
//   return data.species.filter(filterCondition);
// }

// function getEmployee(employeeName) {
//   if (!employeeName) {
//     return {};
//   }
//   const findCondition = (obj) => obj.name === employeeName || obj.lastName === employeeName;
//   return data.employees.find(findCondition);
// }

// function getEmployeeById(employeeId) {
//   if (!employeeId) {
//     return {};
//   }
//   const findCondition = (obj) => obj.name === employeeId || obj.lastName === employeeId;
//   return data.employees.find(findCondition);
// }

function getEmployeesCoverage(info) {
  // const infoData = info.
  // const filterCondition = (element) => ids.includes(element.id) || ids.includes(element.id);
  // return data.species.filter(filterCondition);
}

module.exports = getEmployeesCoverage;
